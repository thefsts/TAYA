kind = "web"
previewPath = "/"
title = "FSTS Client Dashboard"
version = "1.0.0"
id = "artifacts/fsts-dashboard"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19489

[services.development]
run = "pnpm --filter @workspace/fsts-dashboard run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/fsts-dashboard", "run", "build" ]
publicDir = "artifacts/fsts-dashboard/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19489"
BASE_PATH = "/"

{
  "name": "@workspace/visual-regression",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "node scripts/ensure-baselines.mjs && playwright test",
    "test:update": "playwright test --update-snapshots"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "@types/node": "catalog:"
  }
}

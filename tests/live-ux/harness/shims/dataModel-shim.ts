/**
 * @convex/_generated/dataModel shim for the live-UX parent bundle.
 * Type-only module in production; a minimal runtime stub here.
 */
export type Id<T extends string = string> = string & { __brand: T };
export type TableInfo = Record<string, unknown>;
export type DataModel = Record<string, any>;
export const version = 2;
